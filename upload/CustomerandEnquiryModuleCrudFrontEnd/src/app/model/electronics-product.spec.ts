import { ElectronicsProduct } from './electronics-product';

describe('ElectronicsProduct', () => {
  it('should create an instance', () => {
    expect(new ElectronicsProduct()).toBeTruthy();
  });
});
